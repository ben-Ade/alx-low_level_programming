#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int no;
for (no = 0; no < 10; no++);
printf("%d\n", no);
return (0);
}
